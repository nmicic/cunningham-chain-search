# feature_map

Feature extraction for ML-guided search — insufficient signal.

Explored extracting numerical features from CC roots to train a classifier
that could predict which regions contain long chains. The features did not
carry enough predictive signal to justify the overhead.
